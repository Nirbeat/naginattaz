<link rel="stylesheet" href={{profileStyle}}>
<div class="botonera">
    <div class="profile_pic">
        <img src={{userData.profileImg}} alt="Profile picture">
    </div>
    {{#if (eq userData.role "admin")}}
    <a href="/admin/class-views"><button type="button">Admin panel</button></a>
    {{else}}
    {{#if (eq userData.role "free")}}
    <a href="/api/payment/suscription"><button type="button" class="btn-primary">HAZTE DIAMANTE</button></a>   
    {{/if}}
    {{/if}}
    <a href="/api/sessions/logout"><button type="button">Logout</button></a>
</div>